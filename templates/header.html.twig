{# header.html.twig #}
<header class="bg-dark text-white p-3 shadow-sm">
    <div class="container d-flex justify-content-between align-items-center">
        <h1 class="fw-bold">Sklep Internetowy</h1>
        <nav>
            <ul class="nav">
                <li class="nav-item">
                    <a href="index.php" class="nav-link text-white px-4 py-3 rounded-3 me-3 border border-transparent hover-menu">Strona Główna</a>
                </li>
                <li class="nav-item">
                    <a href="index.php?page=cart" class="nav-link text-white px-4 py-3 rounded-3 me-3 border border-transparent hover-menu">Koszyk</a>
                </li>
                {% if zalogowany == true %}
                    <li class="nav-item">
                        <a href="index.php?page=logout" class="nav-link text-white px-4 py-3 rounded-3 me-3 border border-transparent hover-menu">Wyloguj się</a>
                    </li>
                    <li class="nav-item">
                        <span class="text-white">Witaj {{ username }}</span>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a href="index.php?page=login" class="nav-link text-white px-4 py-3 rounded-3 me-3 border border-transparent hover-menu">Zaloguj się</a>
                    </li>
                {% endif %}
            </ul>
        </nav>
    </div>
</header>
