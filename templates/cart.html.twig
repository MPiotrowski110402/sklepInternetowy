{% extends "base.html.twig" %}

{% block title %}Twój Koszyk{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-lg border-0 rounded-lg bg-light text-dark">
                <div class="card-header text-center bg-dark text-white">
                    <h3 class="font-weight-light my-3">🛒 Twój Koszyk</h3>
                </div>
                <div class="card-body">
                    {% if cart|length > 0 %}
                        <table class="table table-striped text-center">
                            <thead class="thead-dark">
                                <tr>
                                    <th>Produkt</th>
                                    <th>Ilość</th>
                                    <th>Cena</th>
                                    <th>Usuń</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for item in cart %}
                                <tr>
                                    <td>{{ item.name }}</td>
                                    <td>{{ item.quantity }}</td>
                                    <td>{{ item.price }} zł</td>
                                    <td>
                                        <a href="index.php?page=cart&id={{ item.id }}" class="btn btn-sm btn-outline-danger">❌</a>
                                    </td>
                                </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                        <div class="text-center mt-4">
                            <h5>Łączna kwota: <strong>{{ totalPrice }} zł</strong></h5>
                        </div>
                    {% else %}
                        <p class="text-center text-muted">Twój koszyk jest pusty 😔</p>
                    {% endif %}
                </div>
                <div class="card-footer text-center">
                    <a href="index.php" class="btn btn-outline-secondary">🛍 Kontynuuj zakupy</a>
                    {% if cart|length > 0 %}
                        <a href="checkout.php" class="btn btn-dark">💳 Przejdź do płatności</a>
                        <a href="index.php?page=cart&deleteCart=true" class="btn btn-outline-danger">🗑 Wyczyść koszyk</a>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
